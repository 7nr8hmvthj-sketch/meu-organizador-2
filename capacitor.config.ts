import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.meuorganizador.app',
  appName: 'Meus Plantões',
  webDir: 'dist/public'
};

export default config;
